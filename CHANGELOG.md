# 更新日志 Changelog

本项目的所有版本更新均记录于此。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

格式约定：

- `新增` — 新功能
- `优化` — 改进 / 体验提升
- `修复` — Bug 修复
- `移除` — 下线的功能

---

## [1.2.1] - 2026-09-05

删除修复版本。

### 修复

- **带下载任务的历史记录删除无效**：解析页历史中，只要记录带有下载任务（下载中 / 已完成 / 暂停 / 等），长按多选删除时数据源返回空列表、删除逻辑也只移走下载任务而从不删除记录本身，导致删不掉。现修复为：选中记录随下载任务（已完成→回收站，未完成→取消清除）一并移入回收站，可随时恢复。

---

## [1.2.0] - 2026-09-05

B 站播放修复与图集多选下载版本。

### 新增

- **图集多选批量下载**：点击下载弹出的图集面板支持任意勾选多张图片批量下载，内置「全选 / 取消全选」；勾选状态仅局部刷新圆圈与按钮计数，不再整页重建，选择过程无闪动。
- **图集面板 iOS 极简风**：底部弹层限高（最高约占屏幕 42%），图片较多时列表内置滑动条滚动查看，面板始终保持紧凑，不撑满屏幕。

### 修复

- **B 站视频播放无声**（核心修复）：
  - 解析层优先请求音视频合一的「整段 mp4」直链（DASH 会把画面与声音拆成两条独立流，纯视频流无声），无整段时再退回 DASH 纯视频流兜底；
  - 补偿 B 站 CDN 域名（`*.bilivideo.com`）Referer 校验：此前播放 / 下载 / 缓存请求缺失 B 站 Referer，被 CDN 直接拒绝（HTTP 403），导致预览无声或无法播放；现于播放器与下载侧同步补齐 Referer 携带，播放、下载、音频均正常。

---

## [1.1.0] - 2026-09-05

视频预览缓存与速度优化版本。

### 新增

- **视频预览本地缓存**：解析加载的视频默认自动缓存到本地（`cacheDir/preview_cache`），再次打开同一视频秒开免加载；以 URL 去重、系统盘自动回收，无需任何存储权限。
- **缓存管理面板**：「我的 → 缓存管理」iOS 极简风底部弹层，支持：
  - 自动缓存开关（默认开启，SharedPreferences 持久化）；
  - 实时查看缓存占用大小；
  - 按时间范围一键清除：近一天 / 近 7 天 / 全部。
- **多段并行解析**：一次粘贴多段分享内容（支持不同平台混合、一行多个链接），自动拆分后在服务端线程池并行解析（上限 4 路并发），解析结果逐卡展示，每段独立预览 / 下载。
- **清除缓存二次确认**：点击清除任意时间范围的缓存，先弹出 iOS 风格确认层（红色「清除」按钮），确认后才真正删除，防止误触。

### 优化

- **下载与缓存提速**：分片并行线程数 12 → 16、最小分片 4MB → 2MB、分片触发阈值 6MB → 4MB（中小视频也能吃满多线程）；超大传输缓冲（64KB → 256KB）；连接 / 读超时收紧（20s/30s → 15s/25s），失败更快回落重试。
- **底部液态玻璃效果**：玻璃快照改为跟随背后内容实时刷新（节流 ~15fps，滚动/切换时模糊下的内容真正"活"起来，不再是静态贴图）；蒙层白色轻调、顶边线更清晰，观感更接近 iOS 毛玻璃。
- **缓存开关预检**：关闭自动缓存时不再拉起后台服务，播放直接走网络直链，无多余开销。
- **多段拆分增强**：分享文案行尾标点（中英文逗号、句号、括号、引号等）不再粘连进 URL；URL 提取正则预编译复用，减少解析开销。

### 修复

- 缓存文件与下载文件路径冲突：缓存独立存放于 `cacheDir/preview_cache` 子目录。
- **首页点按 / 长按媒体闪动**：同页刷新（多选切换 / 解析进度 / 下载状态）与进出详情页不再重播整页淡入+位移动画，仅 Tab 间切换保留阻尼弹簧转场，消除"点一下闪一下"的观感。
- **权限请求连带弹出**：设置页「通知权限」仅申请通知，不再连带拉起电池优化设置页（仅初始化时按需提示）。
- 并行解析进度文案随解析完成段数实时刷新（"正在并行解析 d/t…"），不打断页面操作。
- **播放无声加固**：播放器改用 `AudioAttributes(USAGE_MEDIA/MOVIE)` 替代已废弃的 `setAudioStreamType`，并显式设定媒体音量满值，修复部分新机型 / ROM 下预览无声的问题。
- **播放界面顶部黑块 → 液态玻璃遮罩**：视频预览 / 全屏层 / 图集查看器底色由死黑统一为深蓝黑 `#0E1116`，预览框顶部叠加 54dp 半透深蓝黑渐变遮罩，替代生硬黑边，观感更接近 iOS 毛玻璃。

- **v1.1.0 临时下载**：https://share.lg0304.xyz/f/529dc5cd-1081-4bce-add1-9e91e9e47372 （暂存 3 天自动过期）

---

## [1.0.1] - 2026-09-04

体验优化与细节修复版本。

### 优化

- **“我的”页面重做**：采用分组圆角卡片布局（回收站 / 电池后台运行 / 通知权限 / 关于轻解析），风格与全局 iOS 极简质感完全统一。
- **“关于轻解析”面板**：底部弹层展示应用介绍与开源仓库链接（GitHub），点击直达项目主页，面板风格与全局 ActionSheet 一致。

### 修复

- **收藏多选闪动回顶**：多选时改为仅局部刷新选中圆圈与操作栏计数，不再重建整页列表，选择任意条目后列表不再闪动、也不再跳回顶部。
- **底部状态栏遮挡媒体组件**：图片查看器 / 视频全屏打开时自动隐藏底部玻璃栏，关闭后恢复；详情页内组件不再被底部遮罩挡住。
- **收藏长按下载无效**：收藏项长按操作面板的“下载”现已真实发起下载（视频 / 图集二选一），并接入下载队列与断点续传。
- **收藏页下载实时提示**：收藏条目内新增下载状态区（下载中百分比进度条 / 已暂停 / 下载失败 / 等待 / 已下载），随任务进度通过广播实时局部刷新，不打断列表浏览。
- **全屏播放状态保护**：返回前台 / 下载状态广播到达时，处于视频全屏、图集查看器或弹层打开状态不再重建页面，杜绝播放被打断、全屏上下文丢失的问题。
- **预览播放器资源释放**：离开页面 / 切换 Tab / 应用退出统一 release 预览 MediaPlayer，避免反复进出详情页后播放器资源残留。

---

## [1.0.0] - 2026-09-04

首个正式发布版本。

### 新增

- **媒体解析**：粘贴分享链接一键解析短视频 / 图集，自动识别平台、标题、作者、封面与清晰度。
- **收藏管理**：
  - 收藏列表按分类组织，支持新建 / 移动分类。
  - 收藏媒体**长按弹出操作面板**（风格与全局一致）：下载、移动到分类、取消收藏、删除记录、进入多选。
  - 多选模式支持批量操作。
- **多线程分片下载**：基于 HTTP `Range` 的分块并行下载，配合前台服务保活，实时展示进度通知；支持暂停、恢复、断点续传。
- **回收站**：历史记录软删除可恢复。
- **内置播放器 / 图片查看器**：视频播放、图片缩放手势、翻页浏览、保存到本地。
- **沉浸式动效**：底部导航阻尼弹簧动画（接近临界阻尼），Tab 切换与选中态丝滑流畅；解析页同样应用弹簧动效。

### 优化

- 底部导航重新设计，去掉伪装液态玻璃遮罩，保留全部动态效果，视觉更简洁、滑动更顺畅。
- 下载任务状态可视化（等待 / 下载中 / 暂停 / 完成 / 失败），进度实时刷新。
- 图片加载使用内存缓存，列表滑动更流畅。

### 修复

- **收藏页面返回跳转错误**：点击收藏内容进入详情后返回，不再错误跳回 Tab1「解析」，而是回到进入前的页面与 Tab。
- **收藏操作无效**：修复收藏列表数据 `id` 缺失导致删除、取消收藏等操作无效的问题。
- **液态玻璃模糊异常**：修正模糊半径计算（由 `dp(24) × 降采样倍率` 修正为输出像素空间直接 `24dp`），避免整条底栏糊成雾面。
- 低版本系统（Android 12 以下）兼容：`RenderEffect` 通过反射调用，低版本自动退化为半透明，不再崩溃。
- 多线程下载进度原子计数类型溢出修复（`Int` → `Long`）。

### 技术栈

- 语言 / 框架：Kotlin、Android View 系统（非 Compose）
- 解析能力：原生 `libmediaparse.so`（arm64-v8a）
- 依赖：仅 Kotlin 标准库，其余均为平台 API

---

## 发布信息

- **GitHub 仓库**：https://github.com/ly5201314gjx/MediaParser
- **安装包**：`release/app-release.apk`（约 5MB，支持 Android 7.0+，debug 签名）
- **v1.0.1 临时下载**：https://share.lg0304.xyz/f/86b28558-12b3-4eca-bef1-8894b0c366ed （暂存 3 天自动过期）